# 包体积优化

## 代码混淆与压缩

* **启用 R8/ProGuard**：在 `build.gradle` 中设置 `minifyEnabled true`。
  * **代码压缩**：移除未使用的方法、字段、类和属性。
  * **混淆**：将类名、方法名重命名为短字母（a， b， c），显著减小 Dex 文件大小。
  * **优化**：内联代码、移除无用参数等。
* **优化资源压缩**：设置 `shrinkResources true`。
  * 配合代码压缩，移除项目中未被任何代码引用的资源文件（如图片、布局）。

## 资源优化

* **图片格式转换**：
  * **WebP**：将 PNG 转为 WebP（有损或无损），通常可减少 25%\~35% 体积。Android Studio 支持一键转换。
  * **AVIF**：新一代格式，比 WebP 压缩率更高，但需注意最低 API 版本兼容性（通常需配合 Jetpack Startup 或自行处理降级）。
* **矢量图（Vector Drawable）**：
  * 对于图标类资源，使用 `VectorDrawable`（XML 格式）。相比于多套分辨率的 PNG，单个矢量图文件极小，且适配不同屏幕密度。
  * 注意：`VectorDrawable` 在运行时生成位图，会略微增加 CPU 计算开销，但对体积帮助很大。
* **资源复用与重绘**：
  * 检查是否有相似图片可以通过代码调整颜色（`tint`）复用。
  * 移除未使用的资源（可利用 Android Studio 的 `Refactor > Remove Unused Resources`）。
* **资源打包配置**：
  * 使用 `resConfigs` 剔除不需要的语言包和屏幕密度资源。

```
defaultConfig {
    // 只保留中文和英文，移除 support 库自动带入的其他语言
    resConfigs “zh”, “en”
    // 只保留主流分辨率，移除低分率或高余量资源
    resConfigs “hdpi”, “xhdpi”, “xxhdpi”
}
```


## 依赖库优化

第三方库是体积膨胀的重灾区。

* **审查依赖**：使用 `gradle dependencies` 查看项目依赖树，剔除重复、冗余或过大的库。
* **替换大库**：
  * 如果只是使用极少数功能（如 Glide 的图片加载，但只用了基础功能），考虑使用更轻量级的替代方案，或自行实现简单逻辑。
  * 避免引入全功能库（如 Google Play Services 全量包），改为按需引入具体模块（如 `play-services-maps` 而非 `play-services-base`）。
* **代码缩减规则**：某些库（如 Firebase、OkHttp）有大量方法和资源，确保 ProGuard 规则不会意外保留过多无用代码。


## 原生库（Native Library）优化


针对 `lib/` 目录下的 `.so` 文件。

* **ABI 分包**：这是非常有效的手段。在 `build.gradle` 中配置 `abiFilters`，只打包主流架构（`arm64-v8a`），放弃 `armeabi-v7a`（如果用户量极低）或 `x86`（模拟器可单独处理）。

```
defaultConfig {
    ndk {
        abiFilters ‘arm64-v8a’
    }
}
```


* **Debug 符号剥离**：Release 构建时，配置 `ndk` 剥离调试符号表（strip），通常能减少 30%\~50% 的 so 体积。


## 资源动态加载

将非首屏必须的资源移到服务器按需下载。

* **On-Demand Modules**（Play Feature Delivery / 动态交付）：使用 AAB 将大型功能模块（如相机滤镜、支付模块、高清地图数据）拆分为动态功能模块，用户在首次安装时只下载基础模块，使用时再下载。
* **资产下载**：对于大型资源文件（如模型数据、字体文件、高清视频），在 App 启动后从 CDN 异步下载，不打包进 APK。


## 构建配置优化

* **Debug 与 Release 分离**：
  * Debug 包保留可调试信息，Release 包严格开启压缩。
  * 使用 `debuggable false`。
* **versionCode 与 Split APKs**：如果不使用 AAB，可以配置 `splits`，按屏幕密度或 ABI 拆分成多个 APK 上传应用商店，用户只下载匹配自己设备的版本。


## 字节码与 Dex 优化

* **R 文件内联**：在 Gradle 插件 7.0+ 中，可以使用 `android.enableR8.fullMode=true` 进行更激进的优化。一些开源插件（如 `RInject` 或 `Redex`，Facebook 开源的字节码优化工具）可以将 R 文件中的常量直接内联到代码中，从而移除 R 类，减少 Dex 数量。
* **去除多 Dex**：如果方法数未超过 65536，尽量保持单 Dex，减少多 Dex 带来的管理开销和体积开销。

## 其他细节

* **清理未使用的类和字段**：定期使用 **APK Analyzer**（Android Studio 自带）分析最终的 APK 包。
  * 查看 `resources.arsc` 文件是否过大（可能是字符串资源过多）。
  * 查看是否有重复的图片或 so 文件误打入包中。
* **国际化资源精简**：如果 App 只面向特定地区，移除 `values-*` 文件夹下的多余语言包。
