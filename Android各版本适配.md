# Android各版本适配

## 精准配置三大核心版本



| 版本参数              | 核心作用                                                                                                                                 | 最佳实践与策略                                                                                                                                                     |
| --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **compileSdkVersion** | 告诉Gradle用哪个版本的SDK编译应用。它决定了你**在写代码时可以使用哪些API**。                                                             | **始终使用最新的稳定版**。这能让你立即受益于新API，并提前发现被弃用的旧API，为未来升级做准备。                                                                     |
| **minSdkVersion**     | 应用可以安装的**最低门槛**。低于此版本的设备将无法安装你的应用。                                                                         | 需要权衡用户覆盖面和开发成本。可参考Google Play后台的统计数据，选择一个能覆盖绝大多数（如95%以上）目标用户的版本。                                                 |
| **targetSdkVersion**  | 表明你的应用**在此版本的安卓系统上经过了充分测试**。当设备系统版本高于此值时，系统会启动兼容性行为，模拟旧版本的行为来保证应用正常运行。 | **应始终更新至最新稳定版**。如果不及时更新，当安卓新版本引入行为变更时，你的应用可能会因为运行在“兼容模式”下而出现意外问题，甚至错失新系统的安全增强和功能优化。 |

### 分步骤搞定兼容性适配

适配工作是一个贯穿开发全过程的系统工程，可以按以下步骤进行：

1. **设定底线**：根据产品定位和用户数据，合理设定 `minSdkVersion`。
2. **编译与检查**：将 `compileSdkVersion` 设为最新，利用IDE的Lint工具检查代码中对新API的调用是否低于 `minSdkVersion`，并及时处理警告。
3. **运行时兼容**：对于需要在新版本上使用高级API，但又不能提高 `minSdkVersion` 的情况，务必在代码中进行**运行时检查**。这是最核心的兼容性手段。

```
// 示例：仅在 Android 6.0 (API 23) 及以上系统动态请求权限
if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.M) {
    // 请求运行时权限的代码
    requestPermissions(new String[]{Manifest.permission.CAMERA}, REQUEST_CODE);
} else {
    // 对于低版本系统，权限在安装时已授予，直接执行操作
    openCamera();
}
```

1. **适配新特性与行为变更**：当决定将 `targetSdkVersion` 升级到新版本时，**必须仔细阅读官方的版本行为变更文档**。例如，从Android 6.0开始引入的运行时权限模型，Android 10对分区存储的强制要求，以及后续版本对后台启动Activity的限制、更严格的隐私保护等。忽略这些变更，是导致应用在高版本系统上崩溃或功能异常的常见原因。
2. **全面测试**：使用不同系统版本的模拟器和真机（或云测试平台）对应用进行功能、界面和性能的测试，确保在各种环境下都能稳定运行。
