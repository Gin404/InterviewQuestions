## 有哪些跨端方案

KMP

* **原生渲染** (使用 Jetpack Compose / SwiftUI)
* **共享逻辑，原生UI**。性能最优，与Android原生开发完美衔接，Kotlin语言零成本。
* 适合希望保持原生体验和性能，又想复用业务逻辑的团队。从Android切入较温和。

**Flutter**

* **自绘引擎** (Skia/Impeller)
* **UI一致性极高**，性能卓越，组件库丰富，热重载开发体验好。通过自绘UI实现平台无关性。
* 适合对UI一致性要求高、希望快速多端部署的产品（如初创公司MVP）。注意Dart语言需学习，包体积稍大。

**React Native (RN)**

* **原生渲染** (通过桥接)
* **生态最成熟**，社区庞大，热重载，可用JavaScript/React技术栈。复用部分Web开发经验。
* 适合已有Web/React技术栈的团队，业务逻辑复杂但UI交互相对标准的应用。需注意**通信桥接**可能成为性能瓶颈。

Lynx

* **双线程 JS 执行**（UI 主线程 + 后台线程）
* **自绘引擎 + 原生组件混合**
* **原生支持标准 CSS**（含动画、变量、Grid）
* **Rust 核心引擎** + **PrimJS**（定制 JS 引擎）
* **内置支持 Android/iOS/Web**，一套代码三端运行

## KMP

### 核心机制

1. **多层源集 (Source Sets)**

项目结构天然分层：`commonMain`存放共享业务逻辑；`androidMain`、`iosMain`等存放平台实现。Gradle插件自动处理依赖关系。

这体现了KMP**"共享逻辑，各端实现"**的哲学，开发者只需关注核心业务复用。

2. **中间表示 (IR)**

Kotlin代码首先被编译成与平台无关的IR。根据目标平台，后端将其转为不同产物：JVM字节码、原生二进制（iOS via Kotlin/Native）、JavaScript（Kotlin/JS）或WebAssembly（Kotlin/Wasm）。
这是KMP实现"一次编写，处处运行"的编译基础，类似其他跨端方案的中间码思想。

3. **`expect`/`actual` 机制**

这是KMP的**精髓**。`commonMain`中用`expect`声明接口，各平台模块用`actual`提供实现。

这完美解决了平台差异性问题，如文件读写、获取设备信息等，既保持逻辑共享又保证原生能力。

// todo actual里用什么写？

### KMP和CMP

**Kotlin Multiplatform (KMP)**：是**地基**，提供跨平台编译和逻辑共享能力。

**Compose Multiplatform (CMP)**：是**可选建筑**，基于KMP地基构建的声明式UI框架。

可以只选择KMP共享逻辑，也可以选择KMP+CMP共享绝大部分UI和逻辑。

CMP的渲染方式：它使用**Skiko**（一个Kotlin封装的Skia图形库）进行**自绘**，而不是调用原生UI组件。这保证了UI在各平台的高度一致性。 // todo android上也是这样的？和flutter的区别？

### KMP编译过程

**1. 第一阶段：编译器前端——统一解析，生成IR**

* 编译器的前端负责读取你写的Kotlin源码（包括`commonMain`和各平台的源码），进行词法、语法和语义分析。
* 无论你的代码最终要运行在哪个平台，前端都会将它转换成一种统一的、与平台无关的**中间表示**，也就是**IR（Intermediate Representation）**。这一步确保了所有平台共享同一套编译前端逻辑，大大简化了编译器设计。

**2. 第二阶段：编译器后端——分发目标，生成产物**

* 得到IR这个"通用半成品"后，编译器的后端就开始工作了。它会根据你在Gradle中配置的**目标（Target）**，将IR分发给对应的后端处理。
* **Kotlin/JVM后端**：读取IR，将其编译成Java字节码（`.class`文件），最终打包成`JAR`或`AAR`，运行在JVM或Android（包含Dalvik与ART）上。
* **Kotlin/Native后端**：这是KMP能支持iOS等原生平台的关键。它通过**LLVM**（一个著名的编译器基础设施项目）将IR编译成各平台的原生机器码。产物可以是：

  * 供iOS应用集成的 `.framework`。
  * 供Linux/Windows使用的 `.so` 或 `.dll` 动态库。
  * 在编译过程中，还会生成一种包含IR信息的 **`.klib`**（Kotlin库）文件，用于库的分发和增量编译。
* **Kotlin/JS后端**：将IR编译成JavaScript代码（`.js`），可以在浏览器或Node.js环境中运行。
* **Kotlin/Wasm后端**：这是较新的目标，将IR编译成WebAssembly二进制格式（`.wasm`），以获得在Web端接近原生的性能。

3. IR是啥？

   R是整个编译过程的"中枢神经"。它不是一个模糊的概念，而是一个具体的数据结构——本质上是一棵**抽象语法树（AST）**，但它比源码级别的AST包含更多信息。

```
fun main() {
  println("Hello, World!")
}
```

上面这段的IR大致是：

```
MODULE_FRAGMENT name:<main>
  FILE fqName:<root> fileName:...
    FUN name:main visibility:public ... returnType:kotlin.Unit
      BLOCK_BODY
        CALL 'fun println (message: kotlin.Any?): kotlin.Unit'
          message: CONST String type=kotlin.String value="Hello, World!"
```

## KMP对于鸿蒙的适配

现状：官方暂无支持。大厂有各自的方案，比如腾讯Kuikly，字节ByteKMP。

**需不需要自己写编译器后端？自己写一个鸿蒙类似Kotlin/Native 的新后端？**

不需要！！！编写一个完整的编译器后端（比如为鸿蒙创建一个类似 Kotlin/Native 的新后端）工程量巨大，需要处理指令集、内存模型、与方舟编译器的ABI（应用二进制接口）对接等深层次问题。对于绝大多数开发者和公司来说，这是一个不切实际的方案。

### 适配底层原理：腾讯Kuikly

1. **鸿蒙的运行时基础**：OpenHarmony 4.0+ 的Stage模型基于OpenJDK衍生架构，**完全支持执行标准JVM字节码（.class文件）**。这为KMP的JVM编译产物提供了运行基础。
2. **KMP的开放扩展机制**：KMP本身支持通过 `createTarget` API来复用已有的编译后端能力。因此，我们可以将鸿蒙**定义为一个特殊的“JVM平台”**，直接复用Kotlin/JVM后端生成字节码，而不是从零创建新后端。
3. **`expect/actual` 机制隔离差异**：通过创建 `harmonyMain` 源集，使用 `actual` 实现去调用鸿蒙的原生API（如网络请求、文件存储等），完美地将平台差异隔离在底层。

### 适配底层原理：字节ByteKMP

1. **Kotlin/Native (KN) 作为执行环境**：在鸿蒙平台上，ByteKMP 没有选择将 Kotlin 代码运行在虚拟机（如 JVM）上，而是采用了 **Kotlin/Native** 技术。它能将你的 KMP 代码（包括共享逻辑和针对鸿蒙的 `actual` 实现）直接**编译成鸿蒙原生的 `.so` 动态库**。这就为后续的交互打下了基础。
2. **NAPI 作为通信桥梁**：NAPI 是鸿蒙系统提供的用于 **ArkTS（方舟运行时）与 C/C++ 代码互操作**的标准底层接口。由于 Kotlin/Native 天生支持与 C 交互（通过 `cinterop`），因此，Kotlin 代码编译出的 `.so` 库就可以通过 NAPI，与上层的 ArkTS 代码进行双向通信。整个链路就是 **`Kotlin (in .so) ↔ NAPI (C接口) ↔ ArkTS`**。

**双向交互：ByteKMP 的封装与优化**

直接使用原生 NAPI 进行调用是极其繁琐的（就像我们上次聊到的，需要手动管理 `napi_value`、处理引用和内存），无法投入生产。ByteKMP 的核心工作量就在于对这一层进行了深度的封装和自动化。

1. Kotlin 调用 ArkTS：面向对象的封装

当 Kotlin 代码需要调用已有的 ArkTS 模块（比如抖音鸿蒙版的日志工具 `@douyin/logger`）时，ByteKMP 设计了一套 **`ArkObject` 对象模型**来屏蔽底层细节。

* **原生 NAPI 的痛点**：你需要手动加载模块、获取对象、获取方法、构造参数、调用方法，每一步都涉及复杂的 NAPI 函数调用和内存管理。
* **ByteKMP 的封装**：它将 ArkTS 侧的模块、对象、函数等，在 Kotlin 侧映射为 `ArkModule`、`ArkInstance`、`ArkFunction` 等类，这些类都继承自一个能自动管理 `napi_value` 生命周期的基类 `ArkObject`。

**效果**：调用代码变得非常简洁和直观，就像操作本地 Kotlin 对象一样：

```
// 封装后的调用方式
val loggerModule = ArkModule("@douyin/logger")
val logger = loggerModule.getExportInstance("logger")
val dFunc = logger.getFunction("d")
dFunc.call(arrayOf(arkString("KmpTag"), arkString("Hello from Kotlin!")))
```

2. ArkTS 调用 Kotlin：KSP 驱动的代码生成

当 ArkTS 页面需要调用 KMP 共享的业务逻辑（如网络请求、数据解析）时，ByteKMP 采用了 **KSP + 注解**的方式，自动化生成繁琐的桥接代码。

* **原生 NAPI 的痛点**：你需要在 C++（作为中间层）中为每个要导出的 Kotlin 方法编写一个固定的“桥接函数”，处理参数解析和类型转换，然后将这个桥接函数注册到 NAPI 的 `exports` 对象上，最后还要手写 `.d.ts` 声明文件。流程重复且极易出错。
* **ByteKMP 的封装**：开发者只需在要导出的 Kotlin 函数上添加一个简单的注解，如 `@ArkTsExportFunction`。
* **效果**：在编译期，KSP 插件会自动扫描这些注解，并生成所有必要的桥接代码、注册代码和 TypeScript 声明文件。ArkTS 开发者可以像调用普通 ArkTS 函数一样，直接导入并使用这个 Kotlin 函数。

**更高维度的工程化：不止于交互**

除了核心的跨语言交互，ByteKMP 更是一整套完整的工程化体系。它将能力分层建设，以支持字节跳动内部数百个模块、上百万行代码的跨端复用：

* **宿主集成层**：统一了各平台的集成方式，例如鸿蒙输出 **HAR 包**，iOS 输出 **Framework**，Android 输出 AAR，让集成流程标准化。
* **交互能力层**：除了我们上面详述的基于 NAPI 的 **FFI（外部函数接口）** 框架，它还提供了 **SPI（服务提供者接口）** 的设计模式，允许在 `common` 代码中定义接口，而在鸿蒙的 `actual` 代码中注入具体实现，实现逻辑与实现的分离。
* **容器能力层**：沉淀了大量跨平台的基础库，如网络、图片、埋点、日志等，甚至包括对 **Compose 和 ArkUI 渲染范式的统一抽象**。
* **工具链层**：最关键的是，它让开发者可以**统一在 Android Studio 中完成鸿蒙模块的开发、调试、日志查看和 Crash 定位**，极大地提升了研发效率。

总结来说，ByteKMP 在鸿蒙上的实现，是通过 **Kotlin/Native + NAPI** 的技术组合，并辅以 **面向对象的封装** 和 **编译期代码生成**，将底层复杂的跨语言调用变成了上层简单的接口调用。这背后是一整套工程化体系的支撑，确保了大规模、高性能的跨端代码复用成为可能。

### 腾讯VS字节 总结


| **技术栈依赖** | 重度依赖鸿蒙的Java兼容环境                                                                  | 依赖NAPI，与鸿蒙运行时直接交互                                                    |
| -------------- | ------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| **核心优势**   | **代码复用率极高**。现有Android库（如OKHttp、Gson）可直接复用，无需重写。适合快速移植。     | **性能最优**。无虚拟机和JNI开销，调用链路短，类型安全，内存可控。适合高性能场景。 |
| **性能表现**   | 有JVM和JNI两层开销，首次调用可能有类加载和JIT编译开销。                                     | **无额外开销**。直接机器码执行，NAPI调用高效。                                    |
| **开发体验**   | 开发调试简单，**完全在JVM工具链内**（Android Studio + 现有插件），对Java/Kotlin开发者友好。 | 涉及Kotlin/Native和C interop，调试需同时熟悉LLVM和NAPI工具链，门槛稍高。          |
| **鸿蒙依赖**   | **强依赖**鸿蒙的Java兼容层。若未来版本弱化此层，方案可能受影响。                            | **弱依赖**。只要鸿蒙支持NAPI（系统标准接口），方案就可持续。                      |
| **代码规模**   | 适合逻辑复杂、依赖多、需要快速迁移的大型项目。                                              | 适合核心高性能模块、新项目，或希望彻底脱离JVM束缚的场景。                         |

## RN

React Native的原理可以高度概括为一句话：**它是用JavaScript写业务逻辑，通过一个中间的“桥梁”，最终调用和渲染出真正的原生组件（如Android的View和iOS的UIView）** 。

### 核心思路：JavaScript 驱动原生

React Native 的核心思想是“Learn once, write anywhere”。它并没有像Flutter那样自己绘制UI，而是通过一个中间层，把JavaScript的指令翻译给原生系统去执行 。

#### 1. 两大核心角色与通信

* **JavaScript 线程**：你的所有React代码、业务逻辑都运行在这里。它负责计算UI应该是什么样（虚拟DOM），并处理用户的点击等事件。
* **原生线程**：Android/iOS的主线程。它负责真正的UI渲染和底层能力调用（如蓝牙、地理位置）。当JS线程说“我要一个蓝色的View”，原生线程就真的去创建一个蓝色的 `android.view.View` 或 `UIView`。

#### 2. 经典架构：异步的"桥" (Bridge)

在很长一段时间里，这两个线程之间通信全靠一座 **桥 (Bridge)** 。

* **工作机制**：这座桥的工作模式很像“发微信”。JS线程把要做的操作（比如“创建View”、“设置属性”）打包成一个异步的JSON消息，通过桥发出去。原生线程收到消息后，再解析并执行。反之亦然 。
* **局限性**：这种模式虽然解耦了两端，但也带来了 **“桥接税”** 。每次通信都要序列化/反序列化JSON，而且是异步的。想象一下，当你快速滑动列表时，大量的UI更新消息堵在桥上，就会导致掉帧、响应延迟。

#### 3. 新架构：直接的"手" (JSI)

为了解决性能瓶颈，Meta推出了新架构，其核心就是用 **JSI (JavaScript Interface)** 取代了老旧的Bridge 。

* **工作机制**：JSI的神奇之处在于，它允许JavaScript线程**直接持有**一个C++对象的引用。这意味着，JS代码可以**同步且直接地**调用原生方法，就像调用本地JS函数一样，彻底消灭了序列化开销 。
* **两大组件**：基于JSI，新架构孵化出了 **Fabric**（新的UI渲染系统）和 **TurboModules**（新的原生模块加载系统）。Fabric让UI渲染更流畅，TurboModules则实现了原生模块的懒加载，显著提升应用启动速度 。


## Lynx

### lynx的产物？

原始代码：tsx css；
中间产物：app-service.js，lepus.js
最终产物template.js

### 资源来源

本地gecko -> asset -> cdn

### Lynx是如何运行的？渲染原理？

双线程架构。

UI线程：运行lepus.js，构建virtual dom，js逻辑触发vdom更新，计算diff，并触发自绘或者通过jni调用到java层对应的节点进行更新。

js线程：运行app_service.js，处理业务代码、副作用等逻辑；

### JSB如何实现？

#### js调用native

c++层有个一个NativeModule用于包装java层的jsb。

技术基础：JSI一个很重要的概念是HostObject和HostFunction。HostObject和HostFunction是JS里一类特殊的Object/Function，它们的相应接口被代理到Native侧，在JS侧访问它们的相应接口，就等同于调用到Native侧的代码，从而实现JSBinding的效果


举个例子，java层注册一个BrightnessModule，有一个setBrightness方法。
NativeModule有Module和Method两个概念，它们分别对应上文所述的HostObject和HostFunction。每个Module里有多个Method。如这里举例BrightnessModule的Module name是"BrightnessModule"，Method name是"setBrightness"。

1. 客户端初始化LynxView的时候需要将相应Module的Class注入。如上面BrightnessModule需要容器在初始化LynxView时注入BrightnessModule这个Class。
2. Lynx初始化时会向JS环境中注入一个nativeModuleProxy对象，该对象是HostObject，这个对象是所有NativeModule JSB的入口。
3. JS侧从nativeModuleProxy里获取Module "BrightnessModule"：该动作会执行到Native侧Lynx框架代码，Lynx在客户端注入的所有Class中查找，是否有"BrightnessModule"这个Class，若有则通过反射创建一个"BrightnessModule"的实例并返回一个HostObject。若没有则返回undefined。
4. JS侧访问BrightnessModule里名为"setBrightness"的property：执行到Native侧代码，Lynx会在"BrightnessModule"这个Class里的所有Method里查找，是否有"setBrightness"这个Method，若有则返回一个HostFunction，若无则返回undefined。
5. JS侧传入参数，调用"setBrightness"这个HostFunction：执行到Native侧代码，Lynx会将JS侧传过来的参数转为平台层的参数，将参数传给平台层对应方法并执行。
6. 返回值处理：平台层执行完JSB后将返回值传给Lynx框架，Lynx将平台层参数转为JS类型后返回给JS侧。

#### native调用js

c++层有个一个JSModule，提供api给java层像js侧发送事件，LynxJS框架提供了registerModule方法，可以通过此方法注册任意JSModule到Lynx，JS框架内，平台层通过LynxContext提供的接口，指定JSModule的名字和JSModule的Method名便可直接触发JS侧对应代码的执行。

1. JS侧通过registerModule方法注册JSModule "MyModule"
2. 平台层利用lynxContext提供的接口getJSModule 拿到JSModule，触发JS侧代码执行。
3. Lynx Native框架将平台层参数转为JS参数
4. Lynx Native框架利用JS引擎提供的接口将Module名"MyModule"，Method名"sayHello"，与JS参数一起传到Lynx JS框架侧。
5. Lynx JS框架查找MyModule.sayHello是否存在，若存在则调用，若不存在则报错。

### 动态化怎么做到的？是局部动态化还是全局动态化？

就是template.js的动态化，动态加载。

### 做过哪些优化？

#### 容器创建阶段

1. 创建 LynxView 复用池，减少 CreateView 创建时间。
2. 异步注册 JSB：容器在打开阶段的耗时会随着注册 jsb 的数量增加儿增长，而创建和注册的执行也全部在主线程，当我们 JSB 数量较多时，会严重影响容器的打开耗时
   由于 JSB 方法注册完成，到真正使用到 JSB 的方法有很长一段时间，可以尝试通过一步创建 JSB 方法 + 注册的方式。
3. 预渲染机制。提前初始化并渲染页面，将产物存入缓存池，页面上屏时直接从缓存池中取出渲染好的产物，省去了创建、获取资源、加载资源等一系列过程，提升了页面打开速度。

#### 加载 template.js LoadTemplate 阶段

1. 删除无效引用：
   我们使用 Lyra 框架时，在打包阶段，会将每个 page 打包成一个 template.js 包，所以我们首先要保证的是单个 page 的内聚性，检查 page 中是否引用其他 page 的逻辑或者组件。
   例如如下代码，当在我们的 page 中调用了 getPageUseName 方法时，lynx 打包会把依赖的所有页面都打包进来，极大增加了我们的包大小。
2. 动态 JS / 组件(需要容器侧支持。
   动态 JS / 组件，相当于把一些 js 逻辑或组件拆到一个单独的 js 包里面(变相减少首页 js 包的大小)，在使用的时候去动态加载。
   - 静态资源（lottie、video、audio）拆分 Gecko 独立 channel
     适合用于较大的资源，并且长期不会变动、但又对加载速度比较敏感的场景
   - 图片资源优化
     可以对图片资源进行压缩，使用 webp 等格式
   - 接入Lynx preDecode，在渲染之前提前进行 decode 操作(需要容器侧支持)
   - 开启 enable_code_cache 开关 该功能仅限 Android 使用、提升非常明显，这里举例 V8 引擎的 code cache 功能说明：涉及到V8使用JIT即时编译来执行javascrit代码，所以开销大，可以缓存编译结果，后续跳过编译直接使用缓存的编译结果。
