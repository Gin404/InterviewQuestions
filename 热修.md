# 美团Robust

美团Robust的核心原理可以概括为一句话：**通过「提前埋伏 + 动态转向」的方式，在方法级别实现即时生效的热修复。**

它在代码编译阶段就"未雨绸缪"，为每个方法都预先埋下一个逻辑开关。当需要修复Bug时，只需下发补丁包，这个开关就会将方法调用动态地转向补丁包中的新代码，从而实现**实时生效且无需重启应用**的修复效果。

## **### 第一步：代码插桩 (Instrumentation)**

这是Robust实现热修复的基石。在App的**编译打包阶段**，Robust会自动遍历项目中的所有类，并对每个方法进行"改造"。

```
public class Hotfix {
    public int needToHotfix() {
      return 0; // 原始逻辑
    }
}
```

经过Robust插桩后，它会被动态地修改为：

```
public class Hotfix {
    // 1. 插入一个静态成员变量，这是关键的"开关"
    public static ChangeQuickRedirect changeQuickRedirect; 
  
    public int needToHotfix() {
        // 2. 在方法开头插入一段判断逻辑
        if (changeQuickRedirect != null) {
            // 如果开关不为空，则执行补丁包中的新方法
            return (int) changeQuickRedirect.accessDispatch(...);
        }
        // 3. 否则，继续执行原有逻辑
        return 0; 
    }
}
```

* **核心技术**：Robust使用**ASM**和**Javassist**等字节码操作框架来完成这项精细的插桩工作。你可以在`robust.xml`配置文件中精确控制哪些包或类需要插桩。


## 第二步：补丁生成 (Patch Generation)


当线上App出现Bug后，开发人员在修复Bug的代码基础上，运行自动化补丁生成工具。这个过程并非简单地替换文件，而是一个**智能的"差异对比"过程**：

1. **对比与识别**：工具会将**修复后的代码**与**线上版本的代码**进行精细对比，精准识别出真正发生变动的方法。
2. **生成补丁类**：为每个被修复的方法生成对应的补丁类（例如 `HotfixPatch`），并在其中编写好新的逻辑。
3. **创建补丁包说明**：生成一个名为 `PatchesInfoImpl` 的关键类，它像一个"地图"，明确记录了**哪个类（如 `Hotfix`）的Bug应该由哪个补丁类（如 `HotfixPatch`）来修复**。


## 第三步：补丁加载与生效 (Patch Loading)

补丁包（通常是一个包含上述类的 `.dex` 文件）下发到用户手机后，Robust的运行时机制开始工作：

1. **加载补丁包**：App通过类似`DexClassLoader`的机制加载补丁包。
2. **设置"开关"**：Robust会解析补丁包中的 `PatchesInfoImpl` 类，根据它的指引找到有Bug的原始类（如 `Hotfix`），然后通过反射将其静态变量 `changeQuickRedirect` 赋值为补丁类（`HotfixPatch`）的实例。
3. **即时转向**：当App再次调用 `needToHotfix()` 方法时，方法开头的判断条件 `changeQuickRedirect != null` 成立，于是执行流向被转向到 `HotfixPatch` 中的新代码，Bug就此修复完成，整个过程**用户无感知，无需重启应用**。
