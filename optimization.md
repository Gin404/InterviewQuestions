# TTI统计以及优化方案

## 如何定义TTI指标

### 前期调研

#### 字节内部前端方案（长耗时帧）

FMP开始计算，往后推算Long Task结束时间即时TTI。

#### 快手（长耗时帧）

起点：页面第一帧的时间戳

终点：连续400帧小于耗时小于83ms的第一帧时间戳

#### 美团（有效绘制面积比例）

起点：页面展示的第一帧

终点：屏幕内有效view绘制面积占屏幕比例

#### 总结，两个思路

1. 有效元素占屏幕面积。
2. 找到最后出现的长耗时帧。

### 对于两个方案做自己的实现

#### 基于有效面积

startTime: 页面第一帧时间戳。

endTime:

1. 注册onGlobalLayout回调。
2. 通过获取activity.weindow.decorview.rootView，获取所有叶子节点。
3. 均匀散步100个点在屏幕上，看有多少点命中了叶子节点。
4. 当有一次超过80%，立即停止，这就是tti endtime。

#### 基于最后出现的长耗时帧

startTime: 页面展示第一帧的时间戳。

endTime: 通过Choreographer.getInstance.postFrameCallback，注册回调，在回调里做如下操作：

1. 第一帧回调，记录tti startTime，记录tti endTime；
2. 耗时小于83ms，也就是平稳帧，计数+1；
3. 耗时超过83帧，计数清零，tti endTime计为当前时间；
4. 技术达到统计帧数（60），结束统计，计算tti时间=te-ts。

### 最终决定方案-基于最后出现长耗时帧的方案

#### 为什么选？

基于有效面积，算法复杂度高（广度优先+三层循环）；

根据getText和getImage判断，不一定准；

#### 怎么定义长耗时帧和最大idle帧数？

通过实验找到合适的指标，最终定在最大帧耗时50ms以下，连续80帧，的第一帧。

## 测试排查工具

1. 记录TTI帧耗时，生成柱状图，数据面板。
2. 帧拆解。

## 如何优化

### 高频耗时任务治理

单个短耗时任务虽然不会卡顿，但多个频繁插入主线程消息队列，也会引起卡顿。

1. 日志跳过，减少object.toString的执行。即便release包不执行日志，模板字符串还是会拼接，可以统一用lambda表达式封装。
2. AppLog埋点采样，治理。
3. 复杂场景rv频繁measure和onVisibilityAggregated高频触发。

### 长耗时方法优化

1. MessageBus反射调用优化，编译期生成路由表代替运行时反射生成映射关系。
2. Gson优化，按需反射，缓存复用，编译生成TypeAdapter。
3. View预加载，通过View预加载框架，在activity启动前，就开始预加载。
